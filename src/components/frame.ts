// The drawing frame shared by the histogram and the boxplot. Both put the same
// value at the same place on the screen, so switching between them moves
// nothing but the picture.
export const PAD = { top: 30, right: 14, bottom: 40, left: 46 };
export const W = 720;
export const H = 346;
