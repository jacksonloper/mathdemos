// The drawing frame shared by the histogram and the boxplot, so the chart keeps
// its size and position when the page switches between them. Each chart picks
// its own x range inside it.
export const PAD = { top: 30, right: 14, bottom: 40, left: 46 };
export const W = 720;
export const H = 346;
